<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title> Food Business</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, sans-serif; margin: 2rem; line-height: 1.5; }
        .nav { margin-bottom: 1.5rem; }
        .nav a { display: inline-block; margin-right: 1rem; padding: 0.5rem 1rem; text-decoration: none; background: #007bff; color: white; border-radius: 4px; }
        .nav a:hover { background: #0056b3; }
        .form-group { margin: 1rem 0; }
        label { display: inline-block; width: 120px; font-weight: bold; }
        input, select { padding: 0.4rem; font-size: 1rem; }
        button { padding: 0.5rem 1rem; background: #28a745; color: white; border: none; border-radius: 4px; cursor: pointer; }
        button:hover { background: #218838; }
        .btn-back { background: #6c757d; }
        .btn-back:hover { background: #545b62; }
        .alert { padding: 0.75rem; margin: 1rem 0; border-radius: 4px; }
        .alert-danger { background: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
        ul.errors { list-style: none; padding-left: 0; color: #721c24; }
    </style>
</head>
<body>
    <div class="nav">
        <a href="{{ path('client_index') }}"> Клиенты</a>
        <a href="{{ path('dish_index') }}"> Блюда</a>
        <a href="{{ path('order_index') }}"> Заказы</a>
    </div>
    <h1>{% block title %}{% endblock %}</h1>
    {% block body %}{% endblock %}
</body>
</html>