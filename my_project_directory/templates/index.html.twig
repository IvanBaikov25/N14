{% extends 'base.html.twig' %}
{% block body %}
<h2>Заказы</h2>
<a href="{{ path('order_new') }}">+ Новый заказ</a>
<a href="{{ path('order_export_excel') }}"> Скачать Excel</a>

<table>
<tr><th>ID</th><th>Клиент</th><th>Блюда</th><th>Дата</th><th>Действия</th></tr>
{% for o in orders %}
<tr>
  <td>{{ o.id }}</td>
  <td>{{ o.client.name }} ({{ o.client.phone }})</td>
  <td>{% for d in o.dishes %}{{ d.name }} ({{ d.price }}₽){% if not loop.last %}, {% endif %}{% endfor %}</td>
  <td>{{ o.createdAt|date('d.m.Y H:i') }}</td>
  <td>
    <a href="{{ path('order_edit', {id: o.id}) }}"></a>
    <form method="POST" action="{{ path('order_delete', {id: o.id}) }}" style="display:inline" onsubmit="return confirm('Удалить?')">
      <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ o.id) }}">
      <button type="submit">🗑️</button>
    </form>
  </td>
</tr>
{% endfor %}
</table>
{% endblock %}